import styled from 'styled-components'

const blockquote = styled.blockquote`
  margin-left: 0;
  padding: 1rem;
  border-left: 0.25rem solid ${(props) => props.theme.tertiaryBlueColor};
`

export default blockquote
